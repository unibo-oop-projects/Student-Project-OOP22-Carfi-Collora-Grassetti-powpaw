package powpaw;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import javafx.geometry.Point2D;
import powpaw.model.api.Player;
import powpaw.model.impl.PlayerImpl;

public class PlayerTest {

    @Test
    void getVelocityTest() {
        /* Player player = new PlayerImpl(new Point2D(0, 0), 1);
        player.idle();
        assertEquals(new Point2D(0, 0), player.getVelocity());
        player.moveLeft();
        assertEquals(new Point2D(-10.0, 0), player.getVelocity());
        player.moveRight();
        assertEquals(new Point2D(10.0, 0), player.getVelocity());
        player.jump();
        assertEquals(new Point2D(0, -10.0), player.getVelocity());
        player.idle();
        assertEquals(new Point2D(0, 0), player.getVelocity()); */
    }

}